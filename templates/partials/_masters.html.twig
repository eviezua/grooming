<div class="section-title m-auto container-wide">
    <h1 class="mb-4 text-center mt-5 ">
        <span>{{ 'Choose'|trans }}</span> {{ 'your groomer'|trans }}
    </h1>
</div>
<groomer-catalog :preview="{{ preview ? 'true' : 'false' }}"></groomer-catalog>